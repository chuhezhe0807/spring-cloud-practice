如果服务器宕机，远程服务还可以成功调用吗？
    1、远程调用过：远程调用不再依赖注册中心，可以成功调用。（调用一次后会生成缓存，后续缓存会和注册中心通信，及时更新服务器信息）
    2、没调用过：（第一次发起远程调用，此时还没有缓存）所以不能成功调用。

如果Nacos中的数据集和application.properties有相同的配置项，哪个生效？
    先导入优先，外部优先， Nacos中的数据集会生效

配置中心
    利用Group区分多个微服务